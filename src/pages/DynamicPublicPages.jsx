import { AlertTriangle, Download, ExternalLink, FileText, ImageOff, PlayCircle, RefreshCw, ShieldCheck, Sparkles } from 'lucide-react'
import { useEffect, useState } from 'react'
import { PageSkeleton } from '../components/system/SystemUI'
import { charityFallback, charityMedia } from '../data/charityMedia'

const shell='mx-auto w-full max-w-7xl px-5 sm:px-6 lg:px-8'
function usePublicResource(resource){const [reloadKey,setReloadKey]=useState(0),[state,setState]=useState({loading:true,items:[],error:''});useEffect(()=>{let active=true;setState(current=>({...current,loading:true,error:''}));fetch(`/api/public/${resource}`).then(async response=>{const data=await response.json();if(!response.ok||!data.success)throw new Error(data.message||`Unable to load ${resource}.`);if(active)setState({loading:false,items:data.items||[],error:''})}).catch(error=>{if(active)setState({loading:false,items:[],error:error.message||`Unable to load ${resource}.`})});return()=>{active=false}},[resource,reloadKey]);return {...state,retry:()=>setReloadKey(key=>key+1)}}
function LoadError({message,onRetry}){return <div className="rounded-[2rem] border border-red-100 bg-white p-8 text-center shadow-sm"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-red-50 text-red-600"><AlertTriangle size={22}/></div><h2 className="mt-4 text-xl font-black text-rb-900">Unable to load this content</h2><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-600">{message}</p><button type="button" onClick={onRetry} className="mt-5 inline-flex items-center gap-2 rounded-full bg-rb-900 px-5 py-3 text-sm font-black text-white transition hover:-translate-y-0.5"><RefreshCw size={16}/>Try again</button></div>}

export function DynamicReportsPage(){
  const state=usePublicResource('reports')
  return <main className="bg-[#f7fafc]"><section className="relative isolate overflow-hidden bg-rb-950 text-white"><img src={charityMedia.editorial[2]} alt="RB Charity Foundation reporting and field documentation" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-rb-950 via-rb-950/90 to-rb-900/45"/><div className={`${shell} relative grid min-h-[500px] items-end gap-8 py-16 lg:grid-cols-[1fr_.45fr]`}><div><p className="text-xs font-black uppercase tracking-[.2em] text-gold">Reports & accountability</p><h1 className="mt-5 max-w-4xl text-4xl font-black tracking-[-.05em] sm:text-6xl lg:text-7xl">Documents that make transparency tangible.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/72">Annual, impact and governance reports provide a public record beyond campaign storytelling.</p></div><div className="rounded-[2rem] border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><ShieldCheck className="text-gold"/><h2 className="mt-4 text-xl font-black">Published only when approved</h2><p className="mt-2 leading-7 text-white/65">Reports appear here only after the foundation publishes an approved document through Admin.</p></div></div></section><section className="py-16 sm:py-24"><div className={shell}><div className="mb-10 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.18em] text-rb-600">Document library</p><h2 className="mt-3 text-3xl font-black tracking-[-.04em] text-rb-900 sm:text-4xl">A clear public record.</h2></div><p className="max-w-xl leading-7 text-slate-600">View documents in-browser or download a copy for reference. Missing documents are never represented as published evidence.</p></div>{state.loading?<PageSkeleton cards={4}/>:state.error?<LoadError message={state.error} onRetry={state.retry}/>:state.items.length?<div className="grid gap-5 lg:grid-cols-2">{state.items.map(report=><article key={report._id||report.title} className="group relative overflow-hidden rounded-[2rem] border border-rb-100 bg-white p-6 shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-xl sm:p-7"><div className="absolute -right-8 -top-8 h-28 w-28 rounded-full bg-gold/10"/><div className="relative flex gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-rb-900 text-gold"><FileText/></span><div className="min-w-0"><p className="text-xs font-black uppercase tracking-[.16em] text-rb-600">{report.type||'Report'}{report.year?` · ${report.year}`:''}</p><h2 className="mt-2 break-words text-2xl font-black tracking-[-.03em] text-rb-900">{report.title}</h2>{report.summary&&<p className="mt-3 break-words leading-7 text-slate-600">{report.summary}</p>}</div></div>{report.fileUrl?<div className="relative mt-7 flex flex-col gap-2 sm:flex-row"><a href={report.fileUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center justify-center gap-2 rounded-full bg-rb-900 px-4 py-2.5 text-sm font-black text-white transition hover:-translate-y-0.5"><ExternalLink size={16}/>View report</a><a href={report.fileUrl} download className="inline-flex items-center justify-center gap-2 rounded-full border border-rb-100 bg-white px-4 py-2.5 text-sm font-black text-rb-800 transition hover:bg-rb-50"><Download size={16}/>Download</a></div>:<span className="relative mt-7 inline-flex w-fit rounded-full bg-orange-50 px-3 py-1.5 text-xs font-black uppercase tracking-[.14em] text-orange-700">Document unavailable</span>}</article>)}</div>:<div className="rounded-[2rem] border border-dashed border-rb-200 bg-white p-10 text-center"><FileText className="mx-auto text-rb-500"/><h2 className="mt-4 text-2xl font-black text-rb-900">No reports published yet</h2><p className="mt-2 text-slate-600">Approved foundation reports will appear here automatically.</p></div>}</div></section></main>
}

export function DynamicGalleryPage(){
  const state=usePublicResource('gallery')
  return <main className="bg-[#f7fafc]"><section className="relative isolate overflow-hidden bg-rb-950 text-white"><img src={charityMedia.hero} alt="RB Charity Foundation gallery" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-rb-950 via-rb-950/78 to-transparent"/><div className={`${shell} relative flex min-h-[520px] items-end py-16`}><div className="max-w-4xl"><div className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 text-xs font-black uppercase tracking-[.18em] text-gold backdrop-blur"><Sparkles size={14}/>Field gallery</div><h1 className="mt-5 text-4xl font-black tracking-[-.05em] sm:text-6xl lg:text-7xl">Evidence, energy and moments from the field.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/72">Images and videos create a visual record of community work, campaigns and the people who make each initiative possible.</p></div></div></section><section className="py-16 sm:py-24"><div className={shell}><div className="mb-10 grid gap-4 lg:grid-cols-[1fr_.55fr] lg:items-end"><div><p className="text-xs font-black uppercase tracking-[.18em] text-rb-600">Visual archive</p><h2 className="mt-3 text-3xl font-black tracking-[-.04em] text-rb-900 sm:text-4xl">Field work, without the brochure treatment.</h2></div><p className="leading-7 text-slate-600">Gallery media is published through Admin, keeping public visuals tied to the foundation’s actual field documentation.</p></div>{state.loading?<PageSkeleton cards={6}/>:state.error?<LoadError message={state.error} onRetry={state.retry}/>:state.items.length?<div className="columns-1 gap-4 sm:columns-2 lg:columns-3">{state.items.map((item,index)=>{const mediaType=item.mediaType||'image',mediaUrl=item.mediaUrl||item.image||charityFallback('editorial',index);return <figure key={item._id||`${item.title}-${index}`} className={`group relative mb-4 break-inside-avoid overflow-hidden rounded-[2rem] bg-rb-900 ${index%5===0?'min-h-[420px]':'min-h-[300px]'}`}>{mediaType==='video'&&mediaUrl?<video src={mediaUrl} controls preload="none" className="absolute inset-0 h-full w-full object-cover" aria-label={item.title}/>:mediaUrl?<img src={mediaUrl} alt={item.title||'RB Charity Foundation activity'} loading="lazy" decoding="async" className="absolute inset-0 h-full w-full object-cover transition duration-700 group-hover:scale-105"/>:<div className="absolute inset-0 grid place-items-center bg-rb-800 text-white/40"><ImageOff size={36}/></div>}<div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-rb-950/95 via-rb-900/10 to-transparent"/>{mediaType==='video'&&<div className="pointer-events-none absolute right-5 top-5 grid h-11 w-11 place-items-center rounded-full bg-white/90 text-rb-900 shadow"><PlayCircle size={22}/></div>}<figcaption className="pointer-events-none absolute inset-x-0 bottom-0 p-6 text-white"><p className="text-xs font-black uppercase tracking-[.16em] text-gold">{item.category||'Foundation work'}</p><h2 className="mt-2 text-xl font-black">{item.title||'Foundation activity'}</h2>{item.caption&&<p className="mt-2 text-sm leading-6 text-white/70">{item.caption}</p>}</figcaption></figure>})}</div>:<div className="rounded-[2rem] border border-dashed border-rb-200 bg-white p-10 text-center"><ImageOff className="mx-auto text-rb-500"/><h2 className="mt-4 text-2xl font-black text-rb-900">No gallery items published yet</h2><p className="mt-2 text-slate-600">Published images and videos from the admin panel will appear here automatically.</p></div>}</div></section></main>
}
