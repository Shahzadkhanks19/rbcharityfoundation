import { Edit3, FileText, ImageIcon, LogOut, Menu, Plus, Save, Trash2, Video, X } from 'lucide-react'
import { useEffect, useMemo, useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import CustomSelect from '../components/form/CustomSelect'
import { PageSkeleton } from '../components/system/SystemUI'

const API='/api/admin'
const input='w-full rounded-2xl border border-rb-100 bg-white px-4 py-3.5 text-rb-900 outline-none transition focus:border-gold focus:ring-4 focus:ring-gold/10'
const textarea=`${input} min-h-28 resize-y`
const getToken=()=>sessionStorage.getItem('rbAdminToken')||''
const clearToken=()=>{sessionStorage.removeItem('rbAdminToken');localStorage.removeItem('rbAdminToken')}
async function request(path,options={}){const token=getToken();const res=await fetch(`${API}${path}`,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})}});const data=await res.json();if(!res.ok)throw new Error(data.message||'Request failed');return data}

const nav=[['Dashboard','/admin/dashboard'],['Campaigns','/admin/campaigns'],['Causes','/admin/causes'],['Donations','/admin/donations'],['Donors','/admin/donors'],['Volunteers','/admin/volunteers'],['Partners','/admin/partners'],['Stories','/admin/stories'],['Messages','/admin/messages'],['Gallery','/admin/gallery'],['Reports','/admin/reports'],['Settings','/admin/settings'],['Activity','/admin/activity']]
function Shell({children}){const loc=useLocation(),navigate=useNavigate(),[open,setOpen]=useState(false);useEffect(()=>setOpen(false),[loc.pathname]);const logout=()=>{clearToken();navigate('/admin/login',{replace:true})};const links=nav.map(([label,path])=><Link key={path} to={path} className={`block rounded-2xl px-4 py-3 text-sm font-bold transition ${loc.pathname===path?'bg-gold text-rb-900':'text-white/65 hover:bg-white/10 hover:text-white'}`}>{label}</Link>);return <div className="min-h-screen bg-rb-50 text-rb-900"><aside className="fixed inset-y-0 left-0 z-40 hidden w-64 flex-col bg-rb-900 text-white xl:flex"><div className="border-b border-white/10 px-6 py-6"><Link to="/admin/dashboard" className="text-lg font-black">RB CHARITY</Link><p className="mt-1 text-xs font-bold uppercase tracking-[.18em] text-white/40">Admin panel</p></div><nav className="flex-1 overflow-y-auto p-3">{links}</nav><div className="border-t border-white/10 p-3"><button onClick={logout} className="flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-sm font-bold text-white/70 hover:bg-white/10"><LogOut size={17}/>Logout</button></div></aside><div className="min-h-screen xl:pl-64"><header className="sticky top-0 z-30 border-b border-rb-100 bg-white/95 backdrop-blur"><div className="flex h-[72px] items-center justify-between px-4 sm:px-6 lg:px-8"><button onClick={()=>setOpen(true)} className="grid h-10 w-10 place-items-center rounded-xl bg-rb-50 xl:hidden" aria-label="Open navigation"><Menu size={21}/></button><div className="hidden xl:block"><p className="font-black">RB Charity Admin</p><p className="text-xs text-slate-400">Foundation management console</p></div><button onClick={logout} className="inline-flex items-center gap-2 rounded-full bg-rb-900 px-4 py-2 text-sm font-black text-white"><LogOut size={15}/><span className="hidden sm:inline">Logout</span></button></div></header><main className="min-w-0 p-4 sm:p-6 lg:p-8">{children}</main></div>{open&&<div className="fixed inset-0 z-50 xl:hidden"><button onClick={()=>setOpen(false)} className="absolute inset-0 bg-rb-900/55 backdrop-blur-sm" aria-label="Close navigation"/><aside className="absolute inset-y-0 left-0 flex w-[min(86vw,320px)] flex-col bg-rb-900 text-white shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 p-5"><div><p className="font-black">RB CHARITY</p><p className="text-xs uppercase tracking-[.16em] text-white/40">Admin panel</p></div><button onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-xl bg-white/10"><X size={20}/></button></div><nav className="flex-1 overflow-y-auto p-3">{links}</nav></aside></div>}</div>}

const configs={
 campaigns:{title:'Campaigns',primary:'title',secondary:'summary',statuses:['draft','active','completed','paused','archived'],fields:[['title','Campaign title'],['slug','URL slug'],['summary','Short summary','textarea'],['description','Full description','textarea'],['goalAmount','Funding goal'],['coverImage','Cover image URL'],['startsAt','Start date'],['endsAt','End date'],['featured','Featured campaign','boolean'],['status','Status','status']]},
 causes:{title:'Causes',primary:'name',secondary:'summary',statuses:['draft','published','archived'],fields:[['name','Cause name'],['slug','URL slug'],['summary','Short summary','textarea'],['description','Full description','textarea'],['image','Image URL'],['order','Display order'],['status','Status','status']]},
 stories:{title:'Stories',primary:'title',secondary:'excerpt',statuses:['draft','published','archived'],fields:[['title','Story title'],['slug','URL slug'],['excerpt','Excerpt','textarea'],['content','Story content','textarea'],['coverImage','Cover image URL'],['publishedAt','Publish date'],['status','Status','status']]},
 gallery:{title:'Gallery',primary:'title',secondary:'caption',statuses:['draft','published','archived'],fields:[['title','Title'],['mediaType','Media type','mediaType'],['mediaUrl','Image / video URL'],['category','Category'],['caption','Caption','textarea'],['order','Display order'],['status','Status','status']]},
 reports:{title:'Reports',primary:'title',secondary:'summary',statuses:['draft','published','archived'],fields:[['title','Report title'],['type','Report type'],['year','Year'],['fileUrl','Document URL'],['summary','Summary','textarea'],['status','Status','status']]}
}

export default function AdminCmsPage({resource}){const config=configs[resource],loc=useLocation();const [state,setState]=useState({loading:true,items:[],error:''}),[editor,setEditor]=useState(null),[remove,setRemove]=useState(null);const load=()=>{setState(s=>({...s,loading:true,error:''}));request(`/${resource}`).then(data=>setState({loading:false,items:data.items||[],error:''})).catch(error=>setState({loading:false,items:[],error:error.message}))};useEffect(load,[resource,loc.key]);const sorted=useMemo(()=>[...state.items].sort((a,b)=>(a.order??0)-(b.order??0)),[state.items]);async function quickStatus(item,status){try{await request(`/${resource}/${item._id}`,{method:'PATCH',body:JSON.stringify({status})});load()}catch(error){setState(s=>({...s,error:error.message}))}}async function confirmDelete(){try{await request(`/${resource}/${remove._id}`,{method:'DELETE'});setRemove(null);load()}catch(error){setState(s=>({...s,error:error.message}));setRemove(null)}}return <Shell>{state.loading?<PageSkeleton cards={5}/>:<><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.18em] text-rb-600">Content management</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">{config.title}</h1><p className="mt-2 text-slate-500">Create, edit, publish and organise {config.title.toLowerCase()}.</p></div><button onClick={()=>setEditor({mode:'create',item:{}})} className="inline-flex items-center gap-2 rounded-full bg-gold px-5 py-3 font-black"><Plus size={17}/>Add new</button></div>{state.error&&<p className="mt-5 rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-700">{state.error}</p>}<div className="mt-7 grid gap-4">{sorted.length?sorted.map(item=><article key={item._id} className="grid gap-4 rounded-3xl bg-white p-5 shadow-sm lg:grid-cols-[minmax(0,1fr)_190px_auto] lg:items-center"><div className="min-w-0"><div className="flex flex-wrap items-center gap-3"><h2 className="break-words text-lg font-black">{item[config.primary]||'Untitled'}</h2>{resource==='gallery'&&<span className="inline-flex items-center gap-1 rounded-full bg-rb-50 px-2.5 py-1 text-xs font-bold text-rb-700">{item.mediaType==='video'?<Video size={13}/>:<ImageIcon size={13}/>} {item.mediaType||'image'}</span>}</div><p className="mt-1 line-clamp-2 text-sm leading-6 text-slate-500">{item[config.secondary]||'No description yet.'}</p>{resource==='campaigns'&&<p className="mt-2 text-sm font-bold text-rb-700">Goal: ₹{Number(item.goalAmount||0).toLocaleString('en-IN')}</p>}{resource==='reports'&&<p className="mt-2 text-sm font-bold text-rb-700">{item.type||'Report'}{item.year?` · ${item.year}`:''}</p>}</div><CustomSelect value={item.status||config.statuses[0]} onChange={value=>quickStatus(item,value)} options={config.statuses.map(value=>({value,label:value}))}/><div className="flex items-center gap-2"><button onClick={()=>setEditor({mode:'edit',item})} className="grid h-11 w-11 place-items-center rounded-full border border-rb-100 text-rb-700 transition hover:bg-rb-50" aria-label={`Edit ${item[config.primary]}`}><Edit3 size={17}/></button><button onClick={()=>setRemove(item)} className="grid h-11 w-11 place-items-center rounded-full border border-red-100 text-red-600 transition hover:bg-red-50" aria-label={`Delete ${item[config.primary]}`}><Trash2 size={17}/></button></div></article>):<div className="rounded-3xl border border-dashed border-rb-200 bg-white p-10 text-center"><FileText className="mx-auto text-rb-400"/><h2 className="mt-4 text-xl font-black">No {config.title.toLowerCase()} yet</h2></div>}</div>{editor&&<EditorModal resource={resource} config={config} mode={editor.mode} item={editor.item} onClose={()=>setEditor(null)} onSaved={()=>{setEditor(null);load()}}/>}{remove&&<ConfirmModal title={remove[config.primary]} onCancel={()=>setRemove(null)} onConfirm={confirmDelete}/>}</>}</Shell>}

function EditorModal({resource,config,mode,item,onClose,onSaved}){const initial=Object.fromEntries(config.fields.map(([key,,type])=>[key,type==='boolean'?Boolean(item[key]):formatInitial(key,item[key]) ]));if(resource==='gallery'){initial.mediaType=item.mediaType||'image';initial.mediaUrl=item.mediaUrl||item.image||''}const [form,setForm]=useState(initial),[state,setState]=useState({busy:false,error:''});async function submit(e){e.preventDefault();setState({busy:true,error:''});const payload={...form};['goalAmount','order','year'].forEach(key=>{if(key in payload&&payload[key]!=='')payload[key]=Number(payload[key])});if(resource==='gallery')payload.image=payload.mediaType==='image'?payload.mediaUrl:'';try{await request(mode==='create'?`/${resource}`:`/${resource}/${item._id}`,{method:mode==='create'?'POST':'PATCH',body:JSON.stringify(payload)});onSaved()}catch(error){setState({busy:false,error:error.message})}}return <div className="fixed inset-0 z-[100] overflow-y-auto bg-rb-900/70 p-4 backdrop-blur-sm"><div className="mx-auto my-6 w-full max-w-2xl rounded-[2rem] bg-white p-5 shadow-2xl sm:p-7"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[.16em] text-rb-600">{mode==='create'?'Create':'Edit'}</p><h2 className="mt-1 text-2xl font-black">{config.title}</h2></div><button onClick={onClose} className="grid h-10 w-10 place-items-center rounded-full bg-rb-50" aria-label="Close"><X size={18}/></button></div><form onSubmit={submit} className="mt-6 grid gap-4 sm:grid-cols-2">{config.fields.map(([key,label,type])=><Field key={key} fieldKey={key} label={label} type={type} value={form[key]} statuses={config.statuses} onChange={value=>setForm({...form,[key]:value})}/>)}{state.error&&<p className="sm:col-span-2 rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-700">{state.error}</p>}<div className="sm:col-span-2 flex justify-end gap-3 pt-2"><button type="button" onClick={onClose} className="rounded-full bg-rb-50 px-5 py-3 font-black">Cancel</button><button disabled={state.busy} className="inline-flex items-center gap-2 rounded-full bg-gold px-5 py-3 font-black disabled:opacity-50"><Save size={16}/>{state.busy?'Saving…':'Save'}</button></div></form></div></div>}
function Field({fieldKey,label,type,value,statuses,onChange}){const wide=['summary','description','content','caption','mediaUrl','fileUrl','coverImage','image'].includes(fieldKey);if(type==='textarea')return <label className={`block ${wide?'sm:col-span-2':''}`}><span className="mb-2 block text-sm font-black">{label}</span><textarea className={textarea} value={value||''} onChange={e=>onChange(e.target.value)}/></label>;if(type==='status')return <label className="block"><span className="mb-2 block text-sm font-black">{label}</span><CustomSelect value={value||statuses[0]} onChange={onChange} options={statuses.map(v=>({value:v,label:v}))}/></label>;if(type==='mediaType')return <label className="block"><span className="mb-2 block text-sm font-black">{label}</span><CustomSelect value={value||'image'} onChange={onChange} options={[{value:'image',label:'Image'},{value:'video',label:'Video'}]}/></label>;if(type==='boolean')return <label className="flex items-center justify-between rounded-2xl border border-rb-100 bg-rb-50 px-4 py-3.5"><span className="font-black">{label}</span><button type="button" onClick={()=>onChange(!value)} className={`relative h-7 w-12 rounded-full transition ${value?'bg-rb-900':'bg-slate-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white transition ${value?'left-6':'left-1'}`}/></button></label>;const inputType=fieldKey==='startsAt'||fieldKey==='endsAt'||fieldKey==='publishedAt'?'date':'text';return <label className={`block ${wide?'sm:col-span-2':''}`}><span className="mb-2 block text-sm font-black">{label}</span><input className={input} type={inputType} value={value??''} onChange={e=>onChange(e.target.value)}/></label>}
function formatInitial(key,value){if(!value)return '';if(['startsAt','endsAt','publishedAt'].includes(key)){const date=new Date(value);if(!Number.isNaN(date.getTime()))return date.toISOString().slice(0,10)}return value}
function ConfirmModal({title,onCancel,onConfirm}){return <div className="fixed inset-0 z-[110] grid place-items-center bg-rb-900/70 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-[2rem] bg-white p-6 shadow-2xl"><h2 className="text-2xl font-black">Delete this item?</h2><p className="mt-3 leading-7 text-slate-600">“{title}” will be permanently removed.</p><div className="mt-7 flex justify-end gap-3"><button onClick={onCancel} className="rounded-full bg-rb-50 px-5 py-3 font-black">Cancel</button><button onClick={onConfirm} className="rounded-full bg-red-600 px-5 py-3 font-black text-white">Delete</button></div></div></div>}
